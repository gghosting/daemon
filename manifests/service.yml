apiVersion: v1
kind: Service
metadata:
    name: "gghkurbtest"
    labels:
        app: "gghkurbtest"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "gghkurbtest"